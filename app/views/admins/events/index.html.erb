<%= link_to "予定を追加", new_admins_event_path %>

<%= month_calendar events: @events do |date, events| %>
  <%= date.day %>
  
  <% events.each do |event| %>
    <div>
      <%= link_to event.title, admins_event_path(event.id) %>
    </div>
  <% end %>
  
<% end %>